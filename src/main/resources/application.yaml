spring:
  datasource:
    url: jdbc:postgresql://ingela.com/ingela_access
    username: ingela
    password: Reme6502tva#
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
